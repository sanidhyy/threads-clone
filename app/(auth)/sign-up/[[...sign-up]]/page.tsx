import { SignUp } from "@clerk/nextjs";

// Define a functional component named Page
const Page = () => {
  // Render the SignUp component provided by Clerk for user registration
  return <SignUp />;
};

export default Page;
